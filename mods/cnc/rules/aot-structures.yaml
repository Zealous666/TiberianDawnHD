aot-upgrade-ltnk:
	Tooltip:
		Name: Panzerkanonen-Upgrade

	Buildable:
		BuildPaletteOrder: 10
		# Keine Prerequisites: Tab wird aktiv sobald FACT gebaut ist.
		# Hinweis: Die Engine aktiviert den Tab-Button nur wenn BuildableItems().Any()
		# true ist (ProductionTabsLogic.cs:37). Prerequisites hier würden den Tab
		# erst nach Bau von Airfield+HQ freischalten — nicht mit FACT allein.
		# Für ein hartes Airfield+HQ-Gate: Prerequisites: vehicleproduction, anyhq
		Queue: Upgrade
		BuildLimit: 1
		MoveIntoWorld: false
		Description: Rüstet alle leichten Panzer mit der\n120mm-Kanone des GDI-Kampfpanzers aus.\nBenötigt: Airfield + HQ

	Valued:
		Cost: 100

	RenderSprites:
		Image: aot-upgrade-ltnk

	ProvidesPrerequisite:
		Prerequisite: aot-ltnk-upgrade

	Interactable: